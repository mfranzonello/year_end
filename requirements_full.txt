-r requirements.txt
# 1.51 pins pyarrow <22, which has no Python 3.14 wheel.
streamlit==1.61.1
requests==2.32.5
selenium==4.38.0
# Pymiere is used only by the local Premiere workflow. The project includes a
# distutils compatibility shim below for Python 3.12+.
pymiere==1.4.1
opencv-python==4.13.0.92
cloudinary==1.44.1
graphviz==0.21
hachoir==3.3.0
PyVimeo==1.1.2
pytubefix==10.3.6


# module only needed for Windows shortcuts
pywin32==311; platform_system == "Windows"

# Re-enable distutils for Python 3.12+ (hacky)
setuptools; python_version >= "3.12"
standard-distutils; python_version >= "3.12"

# Environment policy: install project dependencies only in virtual environments.
# The current global Python installation has legacy project packages to remove later;
# do not add new project dependencies there.
